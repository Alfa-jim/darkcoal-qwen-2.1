name: Build and Push to GHCR (qwen-2.1 - network-volume)

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase owner
        id: owner
        run: echo "owner=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL /usr/local/.ghcup
          sudo apt-get clean
          sudo docker system prune -af
          df -h
          echo "Qwen 2.1 FAST - volume-native, no 33GB bake, build ~3 min."

      - name: Build and push qwen-2.1
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          target: final
          build-args: |
            MODEL_TYPE=qwen-2.1
            COMFYUI_VERSION=0.3.43
            CUDA_VERSION_FOR_COMFY=12.6
            USE_NETWORK_VOLUME=true
          tags: |
            ghcr.io/${{ steps.owner.outputs.owner }}/darkcoal-qwen-2.1:latest
